apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chroma-pvc
  namespace: akg-production
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3              # AWS EBS gp3
  resources:
    requests:
      storage: 10Gi
